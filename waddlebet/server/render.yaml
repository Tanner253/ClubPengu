# Render.com Blueprint for deployment
# To deploy: Connect your GitHub repo to Render and it will auto-detect this file

services:
  - type: web
    name: waddlebet-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /










